1. Create ENV.js with URL (graphql server URL & Bearer auth)
2. Install deps: `yarn install` (requires node >=18)
3. Config target image (originPoint and file name) & bot behavior in `bot.js` (atac or protec)
4. Run `node bot.js`
